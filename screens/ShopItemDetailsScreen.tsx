import React, {useState} from "react";
import BaseView from "../components/BaseView";

export default function ShopItemDetailsScreen({ navigation }) {

    return (
        <BaseView>

        </BaseView>
    );
}
